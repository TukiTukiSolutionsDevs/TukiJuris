"""Test helpers package.

Shared assertion helpers imported explicitly in tests — e.g.:
    from tests.helpers.isolation import assert_isolated
    from tests.helpers.sse import assert_sse_yields, SSEEvent
    from tests.helpers.llm import patch_llm_adapter
"""
